/**
 * Constants
 *
 * All practice data fields are stored uniformly in session blocks.
 * The meta block architecture has been removed — algorithm, interaction,
 * nextDueDate, and lineByLineProgress are saved directly in each session
 * record alongside algorithm-specific fields (grade, interval, eFactor, etc.).
 */



